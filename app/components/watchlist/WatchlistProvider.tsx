"use client";

// Convenience re-export so the provider can be imported from its own module.
// The implementation lives alongside the hook in useWatchlist.tsx.
export { WatchlistProvider } from "./useWatchlist";
export type { WatchlistApi } from "./useWatchlist";
